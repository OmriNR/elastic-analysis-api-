services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_DB: storeapi_db
      POSTGRES_USER: storeapi_user
      POSTGRES_PASSWORD: storeapi_pass
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql

  rabbitmq:
    image: rabbitmq:3-management
    restart: always
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      Rabbit__Host: localhost
      Rabbit__Port: 15672
      Rabbit__User: admin
      Rabbit__Password: admin123
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq

  storeapi:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - db
    ports:
      - "5000:80"
    environment:
      ASPNETCORE_ENVIRONMENT: "Development"
      ConnectionStrings__PostgresDb: "Host=db;Database=storeapi_db;Username=storeapi_user;Password=storeapi_pass"

volumes:
  postgres_data:
  rabbitmq_data:
